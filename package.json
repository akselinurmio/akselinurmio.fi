{
  "name": "akselinurmio.fi",
  "private": true,
  "scripts": {
    "build": "rm -rf dist && parcel build src/{,en/}{index,404}.html && cp -R static/. dist/",
    "dev": "parcel src/{,en/}{index,404}.html",
    "prettier:check": "prettier --check --ignore-path .gitignore .",
    "test": "concurrently --group npm:build npm:prettier:check"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20230115.0",
    "concurrently": "^7.6.0",
    "parcel": "^2.8.3",
    "prettier": "^2.8.4",
    "sharp": "^0.31.3",
    "typescript": "^4.9.5",
    "wrangler": "^2.9.0"
  },
  "browserslist": "> 0.5% in FI, not dead"
}
