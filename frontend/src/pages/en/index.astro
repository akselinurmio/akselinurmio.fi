---
import { getEntry } from "astro:content";
import IndexLayout from "../../layouts/IndexLayout.astro";

const entry = await getEntry("home", "en");
if (!entry) throw new Error("Missing content entry: home/en");
---

<IndexLayout entry={entry} />
