---
import { getEntry } from "astro:content";
import IndexLayout from "../layouts/IndexLayout.astro";

const entry = await getEntry("home", "fi");
if (!entry) throw new Error("Missing content entry: home/fi");
---

<IndexLayout entry={entry} />
